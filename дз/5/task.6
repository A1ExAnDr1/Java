пакет  ru.mirea.pr5 ;

import  java.util.Scanner ;

public  class  task_6 {
/ *
        6. Проверка числа на простоту
    Дано натуральное число n> 1. Проверьте, является ли оно простым.
    Программа должна вывести слово ДА, если число простое и НЕТ, если число
    составное. Алгоритм должен иметь сложность O (logn).
    Указание. Понятно, что задача сама по себе нерекурсивна, т.к. проверка
    числа n на простоту никак не сводится к проверке на простоту меньших чисел.
    Поэтому нужно сделать еще один параметр рекурсии: делитель числа, и именно
    по этому параметру и делать рекурсию.
 * /
    public  static  void  main ( String [] args ) {
        Сканер Сканер =  новый  Сканер ( System . In);
        int n = сканер . nextInt ();
        Система . из . println (isPrime (n));
    }

    частное  статическое  логическое значение  isPrime ( int  n ) {
        return isPrime (n, 2 , false );
    }

    частное  статическое  логическое значение  isPrime ( int  n , int  divisor , boolean  flag ) {
        если (делитель == n)
            вернуть  истину ;
        если (n % делитель ==  0 )
            вернуть  ложь ;
        flag = isPrime (n, ++ делитель, флаг);
        флаг возврата ;
    }
}
